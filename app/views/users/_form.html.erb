<%= stylesheet_link_tag "app.css" %>
<div class="wrapper">
<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <div class=form-group">
      <%= f.label(:Имя, for: "inputName", class: "col-sm-2 control-label") %>
      <div class="col-sm-10">
        <%= f.text_field :name %>
      </div>
    </div>
    <div class="form-group">
      <%= f.label(:email, for: "inputEmail", class: "col-sm-2 control-label") %>
      <div class="col-sm-10">
        <%= f.text_field :email %>
      </div>
    </div>
    <div class="form-group">
      <%= f.label(:"Номер телефона", for: "inputEmail3", class: "col-sm-2 control-label") %><br>
      <div class="col-sm-10">
        <%= f.text_field :number %>
      </div>
    </div>
    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
    <%= f.submit("Сохранить", class: "btn btn-default")  %>
        </div>
      </div>
  </div>
<% end %>

</div>


